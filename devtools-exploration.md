#DevTools Exploration Assignment
## Website 1:https://example.com
### 1. What HTML tags are used on the following HTML tags are present:
<html>
<head>
<title>
<meta>
<body>
<div>
<h1>
<p>
<a>
<style>

### 2. What is the page title?
**Example Domain**

### 3. How many headings are there?
**1** heading element ('<h1>').

## Website 2: https://developer.mozilla.org
### 1. Find the navigation menu - what tag is it wrapped in?
'<nav>'

### 2. How is the search bar structured ?
'<form>'

### 3.What happens when you hover over links?
when links are hovered, the CSS styles apply a text decoration style modification (such as an underline) and change the font color to trigger visual feedback.

## Website 3: Google (https://google.com)
 ### 1. Identify 5 different HTML elements
 *'<div>'(division container)
 *'<img>'(logo image file)
 *'<form>'(search interaction form block)
 *'<input>'(text input fields)
 *'<button>'(action button triggers)

 ## 2. Find a form element and list its inputs
 The main homepage feature is a '<form>' component with the following active inputs:
 * '<input type = "search">' (the primary query search input field)
 * '<input type = "submit">' (the button triger to run search submissions)

 ### 3. Take a screenshot of the elements panel
 ![Elements Panel Screenshot](<Elements Panel Screenshot.png>)
